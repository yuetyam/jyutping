import SwiftUI

extension Text {

        /// `Text(verbatim: "•")`
        static let dotMark: Text = Text(verbatim: "•")

        /// `Text(verbatim: ": ")`
        static let separator: Text = Text(verbatim: ": ").foregroundColor(.separator)
}
